use color_eyre::Result;

pub fn init(
    start_port: u16,
    level_seed: String,
    motd_template: String,
    skip_plugins: bool,
    no_copy_bukkit: bool,
    no_copy_spigot: bool,
    no_copy_paper: bool,
) -> Result<()> {
    todo!()
}
